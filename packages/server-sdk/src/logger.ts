// Re-exported from @streamdesk/shared-types so connect-sdk can share the
// same contract without depending on this package (see plugin-runtime.ts).
export type { PluginLogger } from "@streamdesk/shared-types";
